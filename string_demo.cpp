#include<stdio.h>
main()
{
	 char name[10];
	
/*	printf("ENTER INPUT A CHARACTER : ");
	scanf("%s",&name);
	
	printf("\n\n\t %s",name);
	*/
	printf("\n\n\t String : ");
	//scanf("%s",&name); //to input the string without space!
	//printf("%s",name); // to display the string without space!
	gets(name); // to input the string with space!
	puts(name); // to print the string with space!
		
}
